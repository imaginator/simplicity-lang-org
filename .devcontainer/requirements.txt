# MkDocs and Material Theme
mkdocs>=1.5.0
mkdocs-material>=9.0.0

# Required for pymdownx extensions
pymdown-extensions>=10.0.0

# Additional dependencies for specific features
Pillow>=9.0.0  # For image processing
cairosvg>=2.5.0  # For SVG support
markdown-callouts>=0.2.0  # For callouts/admonitions

# Development and testing tools
mkdocs-minify-plugin>=0.6.0  # For optimized builds
mkdocs-redirects>=1.2.0  # For URL redirects if needed

# Linting and formatting (optional but recommended)
black>=22.0.0
pylint>=2.15.0
